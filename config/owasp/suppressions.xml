<?xml version="1.0" encoding="UTF-8"?>
<suppressions xmlns="https://jeremylong.github.io/DependencyCheck/dependency-suppression.1.1.xsd">

  <suppress>
    <notes><![CDATA[
    no newer version of library is available

    postgresql before versions 10.4, 9.6.9 is vulnerable in the adminpack extension,
    the pg_catalog.pg_logfile_rotate() function doesn't follow the same ACLs than pg_rorate_logfile.
    If the adminpack is added to a database,
    an attacker able to connect to it could exploit this to force log rotation]]></notes>
    <gav regex="true">^org\.postgresql:postgresql:42\.2\.2$</gav>
    <cve>CVE-2018-1115</cve>
  </suppress>

  <!-- not used by server -->

  <suppress>
    <notes><![CDATA[Used only for testing not used in prod]]></notes>
    <gav regex="true">^org\.testcontainers:postgresql:.*$</gav>
    <cpe>cpe:/a:postgresql:postgresql</cpe>
  </suppress>

  <suppress>
    <notes><![CDATA[
    This suppresses false positives on Groovy 2.4.x.
    More information can be found on https://github.com/jeremylong/DependencyCheck/issues/1004
    ]]></notes>
    <cpe>cpe:/a:apache:groovy:2.4.15</cpe>
    <cve>CVE-2016-6497</cve>
  </suppress>
</suppressions>
