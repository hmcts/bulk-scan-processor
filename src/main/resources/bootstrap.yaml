spring:
  cloud:
    propertiesvolume:
      prefixed: false
      paths: /mnt/secrets/bulk-scan
      aliases:
        service-POSTGRES-PASS: BULK_SCANNING_DB_PASSWORD
        s2s-secret: S2S_SECRET
