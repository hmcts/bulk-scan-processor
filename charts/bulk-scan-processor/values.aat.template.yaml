java:
  # Don't modify below here
  image: ${IMAGE_NAME}
  ingressHost: ${SERVICE_FQDN}
  environment:
    S2S_NAME: "bulk_scan_processor"
    S2S_URL: "http://rpe-service-auth-provider-aat.service.core-compute-aat.internal"
    DOCUMENT_MANAGEMENT_URL: "http://dm-store-aat.service.core-compute-aat.internal"
    SCAN_ENABLED: "true"
    SCAN_DELAY: "4000"
    REUPLOAD_ENABLED: "true"
    REUPLOAD_DELAY: "4000"
    SPRING_MAIL_HOST: "false"
    NOTIFICATIONS_TO_ORCHESTRATOR_TASK_ENABLED: "true"
    NOTIFICATIONS_TO_ORCHESTRATOR_TASK_DELAY: "3000"
    PROCESS_PAYMENTS_ENABLED: "true"
    STORAGE_BLOB_SELECTED_CONTAINER: "ALL"
    BULK_SCANNING_DB_USER_NAME: bulk_scanner@bulk-scan-processor-aat
    BULK_SCANNING_DB_NAME: bulk_scan
    BULK_SCANNING_DB_HOST: bulk-scan-processor-aat.postgres.database.azure.com
    FLYWAY_URL: jdbc:postgresql://bulk-scan-processor-aat.postgres.database.azure.com:5432/bulk_scan?sslmode=require
    FLYWAY_USER: bulk_scanner@bulk-scan-processor-aat
